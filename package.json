{
  "name": "qa-jira-assistant",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run start --prefix backend\"",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
